# Positioning & Sizing
position=bottom-right
font_size=28
font_size_text=16
text_outline=1
text_outline_thickness=1.5
background_alpha=0.6
round_corners=0

# --- SECTION 1: GRAPHICS (GPU & VRAM) ---
gpu_stats
gpu_temp
gpu_core_clock
gpu_power
vram
proc_vram

custom_text=───────────────────────

# --- SECTION 2: CPU & SYSTEM RAM ---
cpu_stats
cpu_temp
cpu_mhz
cpu_power
ram

custom_text=───────────────────────

# --- SECTION 3: PERFORMANCE ---
fps
frametime
