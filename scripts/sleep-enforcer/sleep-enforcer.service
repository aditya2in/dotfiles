[Unit]
Description=Sleep Enforcer: Automated shutdown (9:30 PM - 5 AM)
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'hour=$$(date +%%-H); min=$$(date +%%-M); if (( hour > 21 || (hour == 21 && min >= 30) || hour < 5 )); then /usr/bin/shutdown -h now; fi'
